# AetherAntiCheat
AetherAntiCheat 是一个专为 Paper 1.8.9 设计的 Minecraft 反作弊插件，核心针对杀戮光环类作弊进行检测。与传统的基于 CPS 阈值或固定规则的方案不同，它引入了轻量级卷积神经网络（CNN），实时分析玩家的战斗行为序列（攻击角度、时序间隔、目标切换等），通过模型推理输出作弊概率。插件具备可训练、可演进的特性——管理员可导出战斗日志，使用提供的 Python 工具重新训练模型并热加载到服务端，无需修改插件代码。性能方面，纯 Java 实现的单次推理耗时低于 0.3 毫秒，对服务器 tick 影响极小。
